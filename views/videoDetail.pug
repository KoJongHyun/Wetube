extends layouts/main

block content
  .video-detail
    .video-player
      video(src=`/${video.fileURL}`, controls)
    .video-info
      if video.creator.id === loggedUser.id
        a(href=routes.editVideo(video.id)) 
          button Edit video
      h5.video-info__title=video.title
      p.video-info__description=video.description
      if video.views === 1
        span.video-info__views 1view
      else
        span.video-info__views #{video.views} views
      .video-auth
        |Uploaded by 
        a(href=routes.userDetail(video.creator.id))=video.creator.name
    .video-comments
      if video.comments.length === 1
        span.video-commtents__number 1 comment
      else
        span.video-commtents__number #{video.comments.length} comments
