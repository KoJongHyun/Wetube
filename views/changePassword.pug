extends layouts/main

block content
  .form-container
    form(action=`/user${routes.changePassword}`, method="post")
      input(type="password" name="password1" placeholder="Current Password")
      input(type="password" name="password3" placeholder="New Password")
      input(type="password" name="password4" placeholder="Verify New Password")
      input(type="submit" value="Change Password")